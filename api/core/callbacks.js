/*global dope window*/

dope.initComponent({
    name: 'core.callbacks',
    init (dope) {
        dope.Callbacks = class Callbacks {
            constructor () {
                this.callbacks = [];
            }
            add (callback) {

            }
        };
    }
});